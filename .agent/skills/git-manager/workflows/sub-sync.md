---
description: [Sub-Workflow] 同步遠端代碼
---

# 同步遠端代碼 (/sub-sync)

1. 確認當前工作分支。
2. 執行 `git fetch --all`。
3. **詢問與確認**: 條列即將執行的同步指令（例如：`git pull --rebase`）。
4. **許可執行**: 獲得使用者明確許可後執行該指令。
5. 顯示目前與遠端的落後/領先狀態。
